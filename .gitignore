node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vite
coverage

# TypeScript incremental build cache — a build artifact that was tracked, so it dirtied the
# working tree on every typecheck and had to be cleared before `tsc -b` could be trusted.
*.tsbuildinfo
